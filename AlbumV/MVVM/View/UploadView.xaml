<UserControl x:Class="AlbumV.MVVM.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AlbumV.MVVM.View" xmlns:viewmodel="clr-namespace:AlbumV.MVVM.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="Upload"
                   Foreground="#E8D8C3"
                   FontSize="28"
                   HorizontalAlignment="Center"
                   Margin="0,10,0,20"/>
        <StackPanel Orientation="Horizontal"
                    Width="800"
                    Height="300">
            <RadioButton Content="Upload Image"
                         Height="20"
                         Background="#E8D8C3"
                         FontSize="14"
                         HorizontalContentAlignment="Center"
                         VerticalContentAlignment="Center"
                         Margin="600,0,0,200"
                         Style="{StaticResource MenuButtonTheme}">
                
            </RadioButton>
        </StackPanel>
    </StackPanel>
</UserControl>
